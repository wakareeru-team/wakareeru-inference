version:
  inference: "0.1.4"
  detector: "grounding-dino"
  classifier: "wakareeru-0.3.0-alpha.1"

device: "auto"

detector:
  model_path: "models/grounding-dino"
  local_files_only: true
  text_prompt: "a train"
  box_threshold: 0.3
  text_threshold: 0.3
  nms_iou_threshold: 0.5
  min_box_score: 0.0
  max_detections: 4

crop:
  padding_ratio: 0.04
  select_policy: "all"
  fallback_policy: "error" # whole_image or error

classifier:
  model_dir: "models/wakareeru-0.3.0-alpha.1"
  local_files_only: true
  top_k: 5

postprocess:
  min_classification_probability: 0.5
  confusion_groups_enabled: false
  confusion_groups: []
